[project]
name = "a11y-e2e"
version = "0.0.0"
description = "end-to-end tests for the android a11y automation service, driven over adb"
requires-python = ">=3.9"
dependencies = ["pytest>=8"]

[tool.pytest.ini_options]
addopts = "-ra"
